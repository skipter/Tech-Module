public class Main {

    public static void main(String[] args) {

        double calculateResult = ((30 + 21) * 0.5 * (35 - 12 - 0.5));
        double pow = Math.pow(calculateResult, 2);
        System.out.println(pow);
    }
}
